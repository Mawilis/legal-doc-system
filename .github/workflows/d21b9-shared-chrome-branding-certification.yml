name: D21B9 Shared Chrome Tenant Branding Presentation Certification

on:
  push:
    branches:
      - legal-ops-completion-map
    paths:
      - "client/src/components/os/wilsyDashboardChromeConfig.js"
      - "client/src/components/os/WilsyOSDashboardChrome.test.jsx"
      - ".github/workflows/d21b9-shared-chrome-branding-certification.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  d21b9-shared-chrome:
    name: D21B9 focused presentation boundary and build
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout exact commit
        uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: client/package-lock.json
      - name: Install exact client dependencies
        working-directory: client
        run: npm ci
      - name: Run exact D21B9 shared chrome certificate
        working-directory: client
        shell: bash
        run: |
          set -uo pipefail
          set +e
          output="$(npm run test:run -- src/components/os/WilsyOSDashboardChrome.test.jsx 2>&1)"
          test_status=$?
          set -e
          printf '%s\n' "$output"
          if [ "$test_status" -ne 0 ]; then
            exit "$test_status"
          fi
          printf '%s\n' "$output" | grep -Eq '4 passed'
          if printf '%s\n' "$output" | grep -Eqi '[1-9][0-9]* skipped|skipped \([1-9]'; then
            echo "D21B9 certification invalid: skipped tests detected"
            exit 1
          fi
      - name: Build production client
        working-directory: client
        run: npm run build
