name: L8-8I Legal Conflict Review Permission Certification

on:
  push:
    branches:
      - legal-ops-completion-map
    paths:
      - "tools/eos/auth/permission_namespace.py"
      - "tests/unit/test_permission_namespace.py"
      - ".github/workflows/l8-8i-conflict-review-permission-certification.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  l8-8i-permission:
    name: L8-8I permission canon direct and static
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout exact commit
        uses: actions/checkout@v4
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Setup Node for pinned Pyright
        uses: actions/setup-node@v4
        with:
          node-version: "20"
      - name: Install bounded dependencies
        run: |
          python -m pip install --upgrade pip==25.3
          python -m pip install pytest==9.1.1
      - name: Compile exact L8-8I permission surfaces
        run: |
          python -m py_compile \
            tools/eos/auth/permission_namespace.py \
            tests/unit/test_permission_namespace.py
      - name: Type-check exact L8-8I permission surfaces
        run: |
          npx --yes pyright@1.1.413 \
            --pythonpath "$(command -v python)" \
            tools/eos/auth/permission_namespace.py \
            tests/unit/test_permission_namespace.py
      - name: Run exact permission canon certificate with no skips
        shell: bash
        run: |
          set -uo pipefail
          set +e
          output="$(python -m pytest -q -rs tests/unit/test_permission_namespace.py 2>&1)"
          pytest_status=$?
          set -e
          printf '%s\n' "$output"
          if [ "$pytest_status" -ne 0 ]; then exit "$pytest_status"; fi
          printf '%s\n' "$output" | grep -Eq '4 passed'
          if printf '%s\n' "$output" | grep -qi 'skipped'; then
            echo "L8-8I permission certification invalid: skipped tests detected"
            exit 1
          fi
